import Layout from '../../components/projects/layout';
import styled from 'styled-components';
import { StaggerWrapper, Stagger } from 'staggered';
import SEO from "../../components/seo"

export default ({ location, children, ...props }) => {
  const staggeredChildren = React.Children.map(children, (child, i) => <Stagger staggerId={i}>{child}</Stagger>)
  const Container = styled.div`
    max-width: 600px;
    margin: auto;
    margin-top: 68px;
    a {
        background: none;
    }
  `;
  return (
    <Layout location={location} {...props}>
      <SEO
        title={"Staggerd Elements React Library"}
        keywords={["staggered", "react", "library", "components"]}
      />
      <Container>
        <div style={{ top: '0', right: '0', position: 'fixed', zIndex: '1' }} dangerouslySetInnerHTML={{ __html: `<a href="https://github.com/bennetthardwick/staggered"><img width="149" height="149" src="https://github.blog/wp-content/uploads/2008/12/forkme_right_red_aa0000.png?resize=149%2C149" class="attachment-full size-full" alt="Fork me on GitHub" data-recalc-dims="1"></a>`}} />
        <StaggerWrapper staggerOnMobile={true}>
        {staggeredChildren}
        </StaggerWrapper>
      </Container>
    </Layout>
  )
};

<p align="center">
	<a href="https://github.com/bennetthardwick/staggered" title="Staggered">
    <img src="https://raw.githubusercontent.com/bennetthardwick/staggered/master/static/staggered.svg?sanitize=true" width="250" alt="Staggered" />
  </a>
</p>

<p align="center">
  A outrageously simple React library for staggering in elements. Refresh this page to see it in action!
</p>

[![NPM](https://img.shields.io/npm/v/staggered.svg)](https://www.npmjs.com/package/staggered) [![JavaScript Style Guide](https://img.shields.io/badge/code_style-standard-brightgreen.svg)](https://standardjs.com)

## Install

```bash
npm install --save staggered
```

## Usage

```tsx
import React from 'react'

import { StaggerWrapper, Stagger } from 'staggered'

const Example  = () => <StaggerWrapper>
  <Stagger staggerId='title'>
    <h1>Title</h1>
  </Stagger>
  <Stagger staggerId='content'>
    <p>
      "..."
    </p>
  </Stagger>
</StaggerWraper>
```

## License

MIT © [bennetthardwick](https://github.com/bennetthardwick)